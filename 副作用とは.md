### 0. スレッド概要

- 扱ったテーマ：
    
    - C#における「副作用」の意味
        
    - バッチ処理（CSV取り込み）での例外処理設計
        
    - リトライ範囲の分離（Download/Clear と Import）
        
    - 失敗CSVの再処理ループ（無限再実行）の防止策
        
    - 復旧（バックアップからのリストア）成功/失敗時の制御フロー（return/throw）
        

---

### 1. 扱ったトピック一覧（時系列）

-  トピック：C#の副作用（side effect）
    
    - 何を扱った：
        
        - 「戻り値以外の外部状態が変わること」を副作用と呼ぶ、という定義を確認
            
        - 外部状態の例：フィールド/プロパティ更新、引数オブジェクトの中身変更、static/シングルトン状態変更、I/O（ファイル/DB/ネットワーク/Console出力）、イベント発火、例外発生など
            
        - 副作用なしの例として「入力→出力のみ（純粋関数的）」を提示
            
    - 結論：
        
        - 副作用＝「評価/実行により、戻り値以外の状態（外の世界）が変化すること」
            
    - 覚えるもの：
        
        - side effect → 副作用 →「戻り値以外の状態変化」
            
        - I/Oや共有状態変更は副作用として扱う（テスト/並行実行/追跡が難しくなる要因）
            
-  トピック：復旧成功時に return する設計の妥当性
    
    - 何を扱った：
        
        - 「復旧が終わったら return が一番きれいか？」を検討
            
        - 復旧失敗時は throw しているため、上位がリトライすると Download から再実行になり得る点を問題視
            
    - 結論：
        
        - 復旧成功時の `return;` は「今回の取り込みは打ち切る」意味として成立する
            
        - ただし throw の伝播で「同一CSVを次回も拾う」経路が残る
            
    - 覚えるもの：
        
        - 復旧成功時 return は可
            
        - 復旧失敗 throw は、上位のリトライ境界次第で “同じ入力を無限に再試行” につながる
            
-  トピック：壊れたCSVを何度も処理してしまう（無限再処理）問題の原因
    
    - 何を扱った：
        
        - `Execute()` の `foreach (var file in files)` 内で `ImportCustomMaster(...)` が例外を投げると、直後の `File.Delete(file.FullName)` に到達しない
            
        - その結果、CSVが残り続け、次回実行でも同じCSVが処理対象になる
            
    - 結論：
        
        - 「例外発生→CSV削除されない→次回も同じCSV→同じ処理を繰り返す」ループが成立する
            
    - 覚えるもの：
        
        - 失敗入力を残す設計は “再試行ループ” の原因になる（削除/隔離/マーキングが必要）
            
-  トピック：リトライ設計（5回リトライは妥当か / Downloadだけでも止めたい）
    
    - 何を扱った：
        
        - 「復旧失敗→throw→次回Downloadから」になってしまうことへの懸念
            
        - 5回リトライは多い、せめてDownloadは繰り返さないようにしたい、という要求
            
        - リトライ単位（境界）を分ける方針：Download/Clearは1回固定、Importだけリトライ
            
    - 結論：
        
        - “リトライの境界” を明確に分けるのが最も安全
            
        - Download/Clear は副作用・コストが大きいのでリトライ対象から外す
            
        - Import（Merge/Delete）だけを限定回数リトライにする
            
    - 覚えるもの：
        
        - リトライは「どこをリトライするか（境界）」が本体
            
        - Download/Clear（副作用大）は原則リトライしない
            
        - Importのみ限定回数リトライ、という分割が現実解
            
-  トピック：失敗CSVの隔離（次回拾わせない）という対策
    
    - 何を扱った：
        
        - 例外発生時にCSVを `_failed` などへ移動/リネームして、次回の対象から外す案
            
        - `foreach(file)` を try/catch で囲い、catchで隔離する形が最小変更で効果が大きい
            
    - 結論：
        
        - “失敗入力を隔離” すれば、同一CSVが次回以降に拾われず、Download/Clearの再実行も抑止できる
            
    - 覚えるもの：
        
        - 失敗時は「throwするだけ」ではなく、入力（CSV）を隔離することで再処理ループを止める
            
-  トピック：提示コード（ImportCustomMaster内で Download/Clear を try の外に置く構成）の評価
    
    - 何を扱った：
        
        - ユーザー提示の構成：
            
            - Download → バックアップ保存 → Clear を先に実行（tryの外）
                
            - try 内で Merge/Delete（Import）を実行
                
            - catch 内で復旧（バックアップから Import）し、成功したら return
                
        - 指摘点：
            
            - Download/Clear が try の外だと、Clear失敗時に catch に入らず復旧できない
                
            - 復旧成功で return すると runDir（バックアップ）が残る（残す/消す方針が必要）
                
            - `Execute()` 側で例外が起きると `File.Delete` されずCSVが残り、次回も再実行される
                
    - 結論：
        
        - 方針（Download/Clearをリトライしない）は合っている
            
        - ただし try 範囲を「バックアップ＆クリア」まで含めるべき
            
        - さらに Execute側で失敗CSV隔離を入れないと、結局次回同じCSVで再処理（→Download/Clear再実行）になる
            
    - 覚えるもの：
        
        - try 範囲は “復旧対象にしたい副作用” まで含める
            
        - 復旧成功時のバックアップ（runDir）を残すか消すか、方針を決める
            
        - 例外時に入力（CSV）を残すなら隔離しないと再処理ループになる
            
